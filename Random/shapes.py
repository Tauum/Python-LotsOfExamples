    #This program will ask the end user to choose one of four shapes (square, rectangle, circle and triangle)
    #The end user will then enter the dimensions required to calculate the area
    #Finally the program should output the result of the calculation

#shapes
s=  'Square'
r = 'Rectangle'
c = 'Circle'
t = 'Traingle'


while True:
    shape = str(input('Enter shape required here (s, r, c, t): '))
    if (shape = s)
        size = input('Enter size required for Square here: ')
           int(size) + int(size) = answer
           print (answer)
        break
     height = input('Enter height required here: ')
